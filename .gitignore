# ── Node modules ──
node_modules/
**/node_modules/

# ── Environment variables (contain secrets / passwords) ──
.env
.env.local
.env.production
.env.development
**/.env
**/.env.local
**/.env.production

# ── Build output ──
frontend/dist/
frontend/.vite/

# ── Logs ──
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── OS files ──
.DS_Store
Thumbs.db
desktop.ini

# ── Editor ──
.vscode/settings.json
.vscode/*.code-workspace
.idea/

# ── PWA dev files ──
frontend/dev-sw.js
frontend/sw.js
frontend/workbox-*.js

# ── Package lock files (optional — keep if you want reproducible installs) ──
# package-lock.json
